{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "pnpm turbo run build --filter=web... --filter=@repo/ui...",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": ".next"
}
