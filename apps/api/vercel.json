{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "pnpm turbo run build --filter=api... --filter=@repo/db...",
  "installCommand": "pnpm install --frozen-lockfile && pnpm --filter @repo/db exec prisma db push --schema ../../apps/api/prisma/schema.prisma --skip-generate && pnpm --filter @repo/db exec tsx src/seed.ts",
  "outputDirectory": ".next"
}
