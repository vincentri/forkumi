{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "pnpm turbo run build --filter=api... --filter=@repo/db...",
  "installCommand": "pnpm install --frozen-lockfile && pnpm db:seed",
  "outputDirectory": ".next"
}
